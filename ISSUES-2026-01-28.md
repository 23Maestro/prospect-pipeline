# Issues - 2026-01-28

## 1) Video progress background refresh returns 502
- Symptom: `/video/progress` refresh fails with HTTP 502 after cache already loaded.
- Impact: Cache UI is fine, but background refresh is noisy and unclear.
- Desired: Either eliminate 502 or suppress it when cache is valid (done: toast/log suppressed).
- Next check:
  - Confirm FastAPI returns 502 when Laravel returns non-JSON or session invalid.
  - Compare request headers/cookies vs working curl for `/videoteammsg/videoprogress`.

## 2) Repeated athlete_main_id resolution for same 20 IDs
- Symptom: Batch resolver repeatedly resolves the same athlete IDs (limit=20) every refresh.
- Evidence: Logs show identical `athlete_main_id resolved` items repeatedly.
- Likely causes:
  - Cache write not persisted (DB path mismatch or write failing).
  - `getCachedAthleteMainId` not reading expected table or key.
- Next check:
  - Query cache table for those athlete IDs and confirm stored `athlete_main_id`.
  - Verify DB file path and that it is not recreated per run.

## 3) Cache assigned date column (not in Raycast UI)
- Need: Add assigned date to cache only (no Raycast UI display).
- Placement: Add column next to `video_due_date` in cache schema.
- Reason: Track assignment for future logic/analysis without cluttering UI.

## 4) Done sort order
- Need: In Done stage, sort by `date_completed` desc (most recent first).
